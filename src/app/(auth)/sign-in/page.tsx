"use client";
import { SignInCard } from "@/features/auth/components/sign-in-card";

const signInPage = () => {
  return <SignInCard />;
};
//only default exports are allowed for pages in next.js
export default signInPage;
